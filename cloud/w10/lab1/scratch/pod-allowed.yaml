apiVersion: v1
kind: Pod
metadata:
  name: test-pod-allowed
  namespace: demo
spec:
  securityContext:
    runAsUser: 1000
  containers:
  - name: test
    image: nginx:1.25.1
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
